#-----------------------------------------  Setup World --------------------------------

WORLD = World();

WORLD.Setup = fun ()
  {
    Add(Background());

    local.message_scroll_config.x = 50;
    local.message_scroll_config.y = 150;
    Add(MessageScroll(local.message_scroll_config));
  };

WORLD.Build();
