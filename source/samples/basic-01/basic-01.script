#-----------------------------------------  Setup World --------------------------------

WORLD = World();

WORLD.Setup = fun ()
  {
    Add(Background());

    Add(MessageScroll());
  };

WORLD.Build();
