
# -------------------------------------------------------------------------
# Array helper functions
# -------------------------------------------------------------------------

Array.Size = fun(array)
  {
    local.size = 0;
    while (array[local.size])
      local.size++;
    return local.size;
  };
